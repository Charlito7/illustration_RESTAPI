server:
  port: 8080

---
spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    url: jdbc:mysql://localhost:3306/illustrationDB
    password: '@Pbax643#'
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'

---
spring:
  profiles: qa
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    url: jdbc:mysql://localhost:3306/illustrationDB
    password: '@Pbax643#'
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'

---
spring:
  profiles: prod
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    url: jdbc:mysql://localhost:3306/illustrationDB
    password: '@Pbax643#'
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
